name: Real Xray Runtime E2E

on:
  workflow_dispatch:
    inputs:
      url:
        description: "Optional Xray Runtime endpoint URL. Falls back to secret XRAY_RUNTIME_E2E_URL."
        required: false
        type: string
      base_path:
        description: "Optional custom runtime base path. Falls back to secret XRAY_RUNTIME_E2E_BASE_PATH."
        required: false
        type: string
      write:
        description: "Create, toggle and delete a temporary VLESS inbound."
        required: true
        default: false
        type: boolean
      port:
        description: "Required when write=true. Use an unused high port on the target Xray Runtime host."
        required: false
        type: string

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  real-xray-runtime-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run real Xray Runtime contract smoke
        env:
          INPUT_XRAY_RUNTIME_E2E_URL: ${{ inputs.url }}
          SECRET_XRAY_RUNTIME_E2E_URL: ${{ secrets.XRAY_RUNTIME_E2E_URL }}
          XRAY_RUNTIME_E2E_TOKEN: ${{ secrets.XRAY_RUNTIME_E2E_TOKEN }}
          INPUT_XRAY_RUNTIME_E2E_BASE_PATH: ${{ inputs.base_path }}
          SECRET_XRAY_RUNTIME_E2E_BASE_PATH: ${{ secrets.XRAY_RUNTIME_E2E_BASE_PATH }}
          XRAY_RUNTIME_E2E_WRITE: ${{ inputs.write }}
          XRAY_RUNTIME_E2E_PORT: ${{ inputs.port }}
        run: |
          export XRAY_RUNTIME_E2E_URL="${INPUT_XRAY_RUNTIME_E2E_URL:-$SECRET_XRAY_RUNTIME_E2E_URL}"
          export XRAY_RUNTIME_E2E_BASE_PATH="${INPUT_XRAY_RUNTIME_E2E_BASE_PATH:-$SECRET_XRAY_RUNTIME_E2E_BASE_PATH}"
          bash scripts/test-xray-runtime-e2e.sh
